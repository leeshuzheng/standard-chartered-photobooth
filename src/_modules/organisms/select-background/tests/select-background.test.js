'use strict';

import SelectBackground from '../select-background';

describe('SelectBackground View', function() {

  beforeEach(() => {
    this.selectBackground = new SelectBackground();
  });

  it('Should run a few assertions', () => {
    expect(this.selectBackground);
  });

});
